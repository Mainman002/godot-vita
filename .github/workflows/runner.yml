name: 🔗 GHA
on: [push, pull_request, workflow_dispatch]

concurrency:
  group: ci-${{github.actor}}-${{github.head_ref || github.run_number}}-${{github.ref}}-runner
  cancel-in-progress: true

jobs:
  static-checks:
    name: 📊 Static
    uses: ./.github/workflows/static_checks.yml

  android-build:
    name: 🤖 Android
    needs: static-checks
    uses: ./.github/workflows/android_builds.yml

  ios-build:
    name: 🍏 iOS
    needs: static-checks
    uses: ./.github/workflows/ios_builds.yml

  javascript-build:
    name: 🌐 JavaScript
    needs: static-checks
    uses: ./.github/workflows/javascript_builds.yml

  linux-build:
    name: 🐧 Linux
    needs: static-checks
    uses: ./.github/workflows/linux_builds.yml

  macos-build:
    name: 🍎 macOS
    needs: static-checks
    uses: ./.github/workflows/macos_builds.yml

  server-build:
    name: ☁ Server
    needs: static-checks
    uses: ./.github/workflows/server_builds.yml

  windows-build:
    name: 🏁 Windows
    needs: static-checks
    uses: ./.github/workflows/windows_builds.yml

  vita-build:
    name: 🎮 PSVita
    needs: static-checks
    uses: ./.github/workflows/vita_builds.yml

  release:
    name: 🚀 Release and Package
    if: startsWith(github.ref, 'refs/tags/')
    needs: [android-build, ios-build, javascript-build, linux-build, macos-build, windows-build, vita-build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      # This specific download ensures the Vita files are in the right sub-folder
      - name: Download Vita artifact
        uses: actions/download-artifact@v4
        with:
          name: vita-template
          path: ./artifacts/vita-template

      - name: Unzip All Editor Artifacts
        run: |
          for dir in ./artifacts/*/; do
            cd "$dir"
            unzip -o *.zip || true
            cd ../../
          done

      - name: Package Vita Templates
        run: |
          mkdir -p ./tpz_staging/

          # 1. Go to the artifact directory and ensure it's unzipped
          cd ./artifacts/vita-template
          unzip -o *.zip || true

          # 2. Copy the REQUIRED files/folders for the Vita template to staging
          # We need eboot.bin, the modules, and the system files
          cp -r eboot.bin module sce_sys ../../tpz_staging/

          # 3. Create the TPZ from the staging directory
          cd ../../tpz_staging
          # The Vita export template expects these files at the root of its zip
          zip -r ../godot-vita-templates.tpz .

      - name: Structure macOS Editor App
        run: |
          mkdir -p Godot.app/Contents/MacOS
          cd ./artifacts/macos-editor
          unzip -o *.zip || true
          cd ../../
          # Using wildcard to handle the binary name
          cp ./artifacts/macos-editor/godot.osx* Godot.app/Contents/MacOS/Godot
          chmod +x Godot.app/Contents/MacOS/Godot
          zip -r Godot_macOS_Editor.zip Godot.app

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            godot-vita-templates.tpz
            Godot_macOS_Editor.zip
            ./artifacts/windows-editor/godot.windows*
            ./artifacts/linux-editor-mono/godot.x11*
